# Быстрые Команды для Replit

## 🚀 Основные команды

### Запуск бота
```bash
python main.py
```

### Запуск с веб-мониторингом
```bash
python web_monitor.py
```

### Автоматический деплой
```bash
python replit_deploy.py
```

## 🔧 Диагностика

### Проверка конфигурации
```bash
python validate_config.py
```

### Тест базы данных
```bash
python test_db.py
```

### Просмотр логов
```bash
tail -f bot.log
```

### Очистка логов
```bash
> bot.log
```

## 📦 Управление зависимостями

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Обновление зависимостей
```bash
pip install --upgrade -r requirements.txt
```

### Проверка установленных пакетов
```bash
pip list
```

## 💾 База данных

### Пересоздание базы данных
```bash
rm bot_database.db
python main.py
```

### Резервная копия базы данных
```bash
cp bot_database.db backup_$(date +%Y%m%d_%H%M%S).db
```

### Просмотр структуры БД
```bash
sqlite3 bot_database.db ".schema"
```

## 🔐 Управление секретами

### Проверка переменных окружения
```bash
env | grep -E "(TELEGRAM|ADMIN)"
```

### Список всех переменных Replit
```bash
env | grep REPL
```

## 📊 Мониторинг

### Проверка процессов
```bash
ps aux | grep python
```

### Использование памяти
```bash
free -h
```

### Использование диска
```bash
df -h
```

## 🌐 Сеть

### Проверка портов
```bash
netstat -tlnp
```

### Тест интернет-соединения
```bash
ping -c 3 api.telegram.org
```

## 🔄 Git (если используется)

### Клонирование репозитория
```bash
git clone <URL> .
```

### Обновление из репозитория
```bash
git pull origin main
```

### Проверка статуса
```bash
git status
```

## 🛠️ Отладка

### Запуск в режиме отладки
```bash
python -u main.py
```

### Проверка синтаксиса
```bash
python -m py_compile main.py
```

### Интерактивная консоль Python
```bash
python -i
```

## 📝 Полезные алиасы

Добавьте в `.bashrc` для удобства:
```bash
alias bot-start="python main.py"
alias bot-monitor="python web_monitor.py"
alias bot-check="python validate_config.py"
alias bot-test="python test_db.py"
alias bot-logs="tail -f bot.log"
alias bot-restart="pkill -f main.py && python main.py"
```

## 🔧 Устранение проблем

### Бот не запускается
```bash
# Проверка конфигурации
python validate_config.py

# Проверка зависимостей
pip install -r requirements.txt

# Очистка и перезапуск
rm bot_database.db
python main.py
```

### Ошибки импорта
```bash
# Переустановка зависимостей
pip uninstall -y -r requirements.txt
pip install -r requirements.txt
```

### Проблемы с базой данных
```bash
# Полная очистка и пересоздание
rm -f bot_database.db bot.log
python test_db.py
python main.py
```

### Always On не работает
1. Проверьте подписку Replit
2. Убедитесь, что проект публичный
3. Добавьте веб-сервер для keep-alive

## 📱 Тестирование бота

### Основные команды для тестирова��ия
1. `/start` - проверка запуска
2. `/events` - проверка расписания
3. `/book` - тест бронирования
4. `/mybookings` - проверка истории
5. `/feedback` - тест отзывов

### Админские команды
1. `/admin_bookings` - управление бронями
2. `/admin_events` - список мероприятий
3. `/event_feedback 1` - отзывы по событию