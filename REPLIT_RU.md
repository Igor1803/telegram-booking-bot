# Запуск Telegram Бота на Replit

Replit - это онлайн IDE, которая позволяет запускать проекты в облаке без установки Python на ваш компьютер.

## 🚀 Быстрый Старт

### Шаг 1: Создание Replit

1. **Перейдите на** [replit.com](https://replit.com)
2. **Зарегистрируйтесь** или войдите в аккаунт
3. **Нажмите** "Create Repl"
4. **Выберите** "Python" как язык
5. **Назовите проект** (например: "telegram-booking-bot")
6. **Нажмите** "Create Repl"

### Шаг 2: Загрузка Файлов

#### Вариант A: Загрузка через интерфейс
1. **Перетащите все файлы** проекта в левую панель Replit
2. **Или используйте кнопку** "Upload file" для каждого файла

#### Вариант B: Клонирование из Git (если доступно)
```bash
git clone <ссылка-на-репозиторий> .
```

### Шаг 3: Установка Зависимостей

В консоли Replit выполните:
```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка Переменных Окружения

1. **Откройте вкладку** "Secrets" (🔒) в левой панели
2. **Добавьте переменные:**
   - **Key:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `ваш_токен_от_botfather`
   
   - **Key:** `ADMIN_IDS`
   - **Value:** `ваш_user_id` (или несколько через запятую)

### Шаг 5: Создание Telegram Бота

Если у вас еще нет бота:

1. **Найдите** [@BotFather](https://t.me/BotFather) в Telegram
2. **Отправьте** `/newbot`
3. **Следуйте инструкциям** для создания бота
4. **Скопируйте токен** и добавьте в Secrets

### Шаг 6: Получение User ID

1. **Найдите** [@userinfobot](https://t.me/userinfobot) в Telegram
2. **Отправьте любое сообщение**
3. **Скопируйте User ID** и добавьте в Secrets

### Шаг 7: Запуск Бота

1. **Убедитесь, что** `main.py` выбран как главный файл
2. **Нажмите зеленую кнопку** "Run" ▶️
3. **Или выполните в консоли:**
   ```bash
   python main.py
   ```

## 📋 Структура Файлов в Replit

После загрузки ваш проект должен выглядеть так:
```
telegram-booking-bot/
├── main.py
├── config.py
├── db.py
├── bot.py
├── states.py
├── requirements.txt
├── README_RU.md
├── REPLIT_RU.md
└── другие файлы...
```

## ⚙️ Настройка Автозапуска

### Создание .replit файла

Создайте файл `.replit` в корне проекта:
```toml
run = "python main.py"
language = "python3"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "python main.py"]
```

### Настройка Always On (Платная функция)

Для непрерывной работы бота:
1. **Перейдите в настройки** Repl
2. **Включите** "Always On"
3. **Оплатите подписку** Replit (если требуется)

## 🔧 Отладка и Мониторинг

### Просмотр Логов
Логи отображаются в консоли Replit в реальном времени.

### Проверка Конфигурации
```bash
python validate_config_ru.py
```

### Тестирование Базы Данных
```bash
python test_db.py
```

### Просмотр Файлов Базы Данных
База данных `bot_database.db` будет создана автоматически при первом запуске.

## 🌐 Публичный Доступ

### Веб-интерфейс (Опционально)
Если хотите добавить веб-интерфейс для мониторинга:

1. **Создайте файл** `web.py`:
```python
from flask import Flask
import threading
import time

app = Flask(__name__)

@app.route('/')
def home():
    return "Telegram Bot is running!"

@app.route('/health')
def health():
    return {"status": "ok", "timestamp": time.time()}

def run_web():
    app.run(host='0.0.0.0', port=8080)

if __name__ == "__main__":
    # Запуск веб-сервера в отдельном потоке
    web_thread = threading.Thread(target=run_web)
    web_thread.daemon = True
    web_thread.start()
    
    # Запуск основного бота
    from main import main
    main()
```

2. **Обновите requirements.txt:**
```
pyTelegramBotAPI==4.14.0
Flask==2.3.3
```

3. **Измените .replit файл:**
```toml
run = "python web.py"
```

## 🔒 Безопасность

### Защита Токенов
- ✅ **Используйте Secrets** для токенов
- ❌ **Не добавляйте токены** в код
- ❌ **Не коммитьте** .env файлы

### Ограничение Доступа
- Сделайте Repl приватным
- Не делитесь ссылкой на Repl с токенами

## 📊 Мониторинг

### Проверка Работы Бота
1. **Отправьте** `/start` вашему боту
2. **Проверьте логи** в консоли Replit
3. **Убедитесь** в отсутствии ошибок

### Статистика Использования
Логи сохраняются в `bot.log` и отображаются в консоли.

## 🛠️ Устранение Проблем

### Бот не запускается
1. **Проверьте** переменные в Secrets
2. **Убедитесь** в правильности токена
3. **Проверьте** установку зависимостей

### Ошибки в консоли
```bash
# Переустановка зависимостей
pip install --upgrade -r requirements.txt

# Проверка конфигурации
python validate_config.py

# Очистка базы данных
rm bot_database.db
python main.py
```

### Бот периодически останавливается
- **Используйте Always On** (платная функция)
- **Или настройте внешний мониторинг** (UptimeRobot, etc.)

## 💡 Полезные Советы

### Разработка
- Используйте консоль для отладки
- Проверяйте логи регулярно
- Тестируйте изменения перед деплоем

### Производительность
- Replit имеет ограничения по ресурсам
- Для высоконагруженных ботов рассмотрите VPS

### Резервное Копирование
- Регулярно скачивайте `bot_database.db`
- Сохраняйте копии кода локально

## 🔄 Обновление Бота

1. **Остановите** текущий процесс (Ctrl+C)
2. **Загрузите новые файлы**
3. **Обновите зависимости** (если нужно)
4. **Запустите снова**

## 📞 Поддержка

При проблемах с Replit:
- [Документация Replit](https://docs.replit.com)
- [Сообщество Replit](https://replit.com/community)
- [Поддержка Replit](https://replit.com/support)

---

**Готово!** Ваш Telegram бот теперь работает в облаке 24/7 (с подпиской Always On).