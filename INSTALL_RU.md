# Подробная Инструкция по Установке

## Пошаговая Установка Telegram Бота для Бронирования Билетов

> 🌐 **Для установки на Replit (облако):** см. [REPLIT_RU.md](REPLIT_RU.md)

## Локальная Установка

### Шаг 1: Установка Python

#### Windows:
1. Перейдите на [python.org/downloads](https://python.org/downloads/)
2. Скачайте Python 3.10 или новее
3. **ВАЖНО**: При установке обязательно отметьте галочку "Add Python to PATH"
4. Запустите установщик и следуйте инструкциям
5. Проверьте установку, открыв командную строку и введя:
   ```cmd
   python --version
   ```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3 python3-pip
```

#### macOS:
```bash
# Используя Homebrew
brew install python3
```

### Шаг 2: Скачивание Проекта

1. Скачайте все файлы проекта в одну папку
2. Или клонируйте репозиторий (если доступен):
   ```bash
   git clone <repository-url>
   cd telegram-booking-bot
   ```

### Шаг 3: Создание Telegram Бота

1. **Откройте Telegram** и найдите [@BotFather](https://t.me/BotFather)
2. **Отправьте команду** `/start`
3. **Создайте нового бота** командой `/newbot`
4. **Введите имя бота** (например: "Билеты Бот")
5. **Введите username бота** (например: "tickets_booking_bot")
6. **Скопируйте токен** - это строка вида: `123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooP`

### Шаг 4: Получение ID Администратора

1. **Найдите бота** [@userinfobot](https://t.me/userinfobot) в Telegram
2. **Отправьте любое сообщение** боту
3. **Скопируйте ваш User ID** - это число вида: `123456789`

### Шаг 5: Установка Зависимостей

#### Автоматическая установка (Windows):
```cmd
setup_ru.bat
```

#### Ручная установка:
```bash
pip install -r requirements.txt
```

### Шаг 6: Настройка Конфигурации

#### Вариант 1: Переменные окружения

**Windows:**
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
set ADMIN_IDS=ваш_user_id
```

**Linux/Mac:**
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_от_botfather"
export ADMIN_IDS="ваш_user_id"
```

#### Вариант 2: Файл .env (Рекомендуется)

1. **Скопируйте файл примера:**
   ```cmd
   copy .env.example .env
   ```

2. **Отредактируйте файл .env:**
   ```
   TELEGRAM_BOT_TOKEN=123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooP
   ADMIN_IDS=123456789
   DB_PATH=bot_database.db
   ```

### Шаг 7: Проверка Конфигурации

```bash
python validate_config_ru.py
```

Если все настроено правильно, вы увидите:
```
✅ Токен бота: 123456789:...
✅ ID администраторов: [123456789]
✅ Путь к базе данных: bot_database.db
🎉 Конфигурация корректна!
```

### Шаг 8: Запуск Бота

#### Автоматический запуск (Windows):
```cmd
run_ru.bat
```

#### Ручной запуск:
```bash
python main.py
```

### Шаг 9: Тестирование

1. **Найдите вашего бота** в Telegram по username
2. **Отправьте команду** `/start`
3. **Проверьте основные функции:**
   - Просмотр расписания
   - Создание брони
   - Просмотр своих броней

## Устранение Проблем

### Ошибка: "Python не найден"
- Переустановите Python с галочкой "Add to PATH"
- Перезагрузите командную строку

### Ошибка: "Неверный токен бота"
- Проверьте токен от @BotFather
- Убедитесь, что нет лишних пробелов
- Токен должен содержать двоеточие (:)

### Ошибка: "Модуль не найден"
- Запустите: `pip install -r requirements.txt`
- Проверьте, что pip работает: `pip --version`

### Бот не отвечает
- Проверьте интернет-соединение
- Убедитесь, что токен правильный
- Проверьте логи в файле `bot.log`

### Функции администратора не работают
- Проверьте, что ваш User ID в ADMIN_IDS
- Получите ID через @userinfobot
- Перезапустите бота после изменения настроек

## Дополнительные Настройки

### Изменение Базы Данных
```
DB_PATH=custom_database.db
```

### Несколько Администраторов
```
ADMIN_IDS=123456789,987654321,555666777
```

### Логирование
Логи сохраняются в файл `bot.log` автоматически.

## Обновление Бота

1. Остановите бота (Ctrl+C)
2. Обновите файлы проекта
3. Запустите снова: `python main.py`

База данных и настройки сохранятся автоматически.

## Резервное Копирование

Регулярно создавайте копии:
- `bot_database.db` - база данных
- `.env` - настройки
- `bot.log` - логи

## Поддержка

При возникновении проблем:
1. Проверьте файл `bot.log`
2. Запустите `python validate_config_ru.py`
3. Убедитесь, что все зависимости установлены
4. Проверьте интернет-соединение